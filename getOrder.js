const { initialize } = require('@medusajs/order');
(async () => {
    // Or we can just use the Medusa client if it's already installed. 
    console.log("We need to query the database using pg or knex.");
})();
