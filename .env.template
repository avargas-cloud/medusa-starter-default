STORE_CORS=http://localhost:8000,https://docs.medusajs.com
ADMIN_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
AUTH_CORS=http://localhost:5173,http://localhost:9000,https://docs.medusajs.com
REDIS_URL=redis://localhost:6379
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
DATABASE_URL=
DB_NAME=medusa-v2

# QuickBooks Tax Codes
QB_DEFAULT_SALES_TAX_CODE="Sale Tax 7%"

# POS access levels: emails (comma-separated) que son OWNER del POS.
# Vacía o ausente => NADIE es owner: nadie puede otorgar Accounting
# (los grants ya otorgados en pos_accounting_grant siguen funcionando).
POS_OWNER_EMAILS=
