{
    "build": {},
    "deploy": {
        "startCommand": "PLAYWRIGHT_BROWSERS_PATH=/app/.medusa/server/.playwright-browsers npx playwright install chromium --quiet && yarn deploy",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 600
    }
}