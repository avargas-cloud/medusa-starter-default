#!/usr/bin/env node
/**
 * post-build.js
 * Runs after `medusa build` completes on Railway and locally.
 * Add any post-build steps here (e.g. copying assets, env validation, etc.)
 */

console.log("✅ post-build.js: Build completed successfully.");
process.exit(0);
